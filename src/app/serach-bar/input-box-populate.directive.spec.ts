import { InputBoxPopulateDirective } from './input-box-populate.directive';

describe('InputBoxPopulateDirective', () => {
  it('should create an instance', () => {
    const directive = new InputBoxPopulateDirective();
    expect(directive).toBeTruthy();
  });
});
