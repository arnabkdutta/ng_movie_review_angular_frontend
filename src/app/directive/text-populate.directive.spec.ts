import { TextPopulateDirective } from './text-populate.directive';

describe('TextPopulateDirective', () => {
  it('should create an instance', () => {
    const directive = new TextPopulateDirective();
    expect(directive).toBeTruthy();
  });
});
